{
  // Denna tsconfig ska användas som `--project`-parameter
  // då tsc används, för att bara src/ ska transpileras
  "extends": "./tsconfig.base.json",
  "exclude": [
    "node_modules",
    "src/modules/*.ignore",
    "test/**/*.ts"
  ],
  "include": [
    "src/**/*.ts"
  ]
}