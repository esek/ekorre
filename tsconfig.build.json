{
  // Denna tsconfig ska användas som `--project`-parameter
  // då tsc används
  "extends": "./tsconfig.base.json",
  "exclude": [
    "node_modules",
    "src/modules/*.ignore",
    "test/**/*.ts"
  ],
  "include": [
    "src/**/*.ts"
  ]
}