# import User from 'user.graphql'
# import Post from 'post.graphql'

type Query {
    userAccess(username: String!): Access
    postAccess(name: [String!]): Access
    roleAccess(role: [Role!]): Access
}

type Access {
    door: [String]
    web: [String]
}