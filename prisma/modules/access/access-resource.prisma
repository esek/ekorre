model PrismaAccessResource {
	slug String @id
	name String
	description String
	resouceType PrismaResourceType

	@@map("access_resources")
}

enum PrismaResourceType {
	DOOR
	WEB

	@@map("resource_types")
}